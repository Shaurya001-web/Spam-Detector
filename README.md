# Spam-Detector
A machine learning project that classifies messages as spam or ham using NLP techniques. Text data is preprocessed and transformed using TF-IDF for feature extraction. A Random Forest classifier is trained to make predictions with high accuracy. Includes evaluation using confusion matrix, ROC curve, and other performance metrics.
